name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: '${{ github.workflow }} @ ${{ github.ref }}'
  cancel-in-progress: false

jobs:
  release:
    name: Release
    permissions:
      contents: write
      packages: write
    uses: iu2tzo/reusable-actions/.github/workflows/goreleaser.yaml@01dfa5b8bca8336ab047d196c367ef4cc9025432 # v1.3.1
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
